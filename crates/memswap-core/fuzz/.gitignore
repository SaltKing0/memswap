# Generated by cargo fuzz — never commit.
#
# `seeds/` holds the checked-in starting corpus. Seed a fresh run by copying
# it into the working corpus first — pointing the fuzzer straight at `seeds/`
# makes libFuzzer write its new units back into the repo:
#
#   mkdir -p corpus/index_json && cp seeds/index_json/* corpus/index_json/
#   cargo +nightly fuzz run index_json
corpus/
artifacts/
coverage/
target/